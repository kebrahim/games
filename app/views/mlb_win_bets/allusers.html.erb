<div class='row-fluid'><div class='span12 center'>

<h3>MLB Over/Unders <%= @currentYear %></h3>

<div class='row-fluid'>
  <div class='span5 center'>
    <h4>My Bets</h4>
    <table class='table table-striped table-bordered table-condensed center'>
      <thead>
        <tr>
          <th>MLB Team</th>
          <th>Line</th>
          <th>Prediction</th>
          <th>Bet Amount</th>
        </tr>
      </thead>
      <% @userBets.each do |bet| %>
        <tr>
          <td><%= bet.mlb_win.mlb_team.abbreviation %></td>
          <td><%= bet.mlb_win.line.to_s %></td>
          <td><%= bet.prediction %></td>
          <td><%= bet.amount.to_s %></td>
        </tr>
      <% end %>
    </table>
    <% if @betsEditable %>
      <a class='btn btn-primary' href='mlbOverUnderBets'>Change My Bets</a><br/><br/>
    <% end %>
  </div>

  <div class='span7 center'>
    <% last_user_id = -1 %>
    <% first_user = true %>
    
    <h4>Standings</h4>
    <% @allBets.each do |bet| %>
      <% if last_user_id != bet.user_id %>
        <% last_user_id = bet.user_id %>
        <% if first_user == false %>
          </table>
        <% else %>
          <% first_user = false %>
        <% end %>

        <h5><%= bet.user.fullName %></h5>
        <table class='table table-striped table-bordered table-condensed center'>
          <thead>
            <tr>
              <th>MLB Team</th>
              <th>Line</th>
              <th>Prediction</th>
              <th>Bet Amount</th>
            </tr>
          </thead>
      <% end %>
            <tr>
            	<td><%= bet.mlb_win.mlb_team.abbreviation %></td>
            	<td><%= bet.mlb_win.line.to_s %></td>
            	<td><%= bet.prediction %></td>
            	<td><%= bet.amount.to_s %></td>
            </tr>
    <% end %>
        </table>
  </div>
</div>

</div></div>